// Code generated by goa, DO NOT EDIT.
//
// enduro HTTP client CLI support package
//
// Command:
// $ goa gen github.com/artefactual-labs/enduro/internal/api/design -o
// internal/api

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	batchc "github.com/artefactual-labs/enduro/internal/api/gen/http/batch/client"
	collectionc "github.com/artefactual-labs/enduro/internal/api/gen/http/collection/client"
	pipelinec "github.com/artefactual-labs/enduro/internal/api/gen/http/pipeline/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"pipeline (list|show|processing)",
		"batch (submit|status|hints)",
		"collection (monitor|list|show|delete|cancel|retry|workflow|download|decide|bulk|bulk-status)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "pipeline list --name \"abc123\" --status false" + "\n" +
		os.Args[0] + " " + "batch submit --body '{\n      \"completed_dir\": \"abc123\",\n      \"depth\": 1,\n      \"exclude_hidden_files\": false,\n      \"path\": \"abc123\",\n      \"pipeline\": \"abc123\",\n      \"process_name_metadata\": false,\n      \"processing_config\": \"abc123\",\n      \"reject_duplicates\": false,\n      \"retention_period\": \"abc123\",\n      \"transfer_type\": \"abc123\"\n   }'" + "\n" +
		os.Args[0] + " " + "collection monitor" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		pipelineFlags = flag.NewFlagSet("pipeline", flag.ContinueOnError)

		pipelineListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		pipelineListNameFlag   = pipelineListFlags.String("name", "", "")
		pipelineListStatusFlag = pipelineListFlags.String("status", "", "")

		pipelineShowFlags  = flag.NewFlagSet("show", flag.ExitOnError)
		pipelineShowIDFlag = pipelineShowFlags.String("id", "REQUIRED", "Identifier of pipeline to show")

		pipelineProcessingFlags  = flag.NewFlagSet("processing", flag.ExitOnError)
		pipelineProcessingIDFlag = pipelineProcessingFlags.String("id", "REQUIRED", "Identifier of pipeline")

		batchFlags = flag.NewFlagSet("batch", flag.ContinueOnError)

		batchSubmitFlags    = flag.NewFlagSet("submit", flag.ExitOnError)
		batchSubmitBodyFlag = batchSubmitFlags.String("body", "REQUIRED", "")

		batchStatusFlags = flag.NewFlagSet("status", flag.ExitOnError)

		batchHintsFlags = flag.NewFlagSet("hints", flag.ExitOnError)

		collectionFlags = flag.NewFlagSet("collection", flag.ContinueOnError)

		collectionMonitorFlags = flag.NewFlagSet("monitor", flag.ExitOnError)

		collectionListFlags                   = flag.NewFlagSet("list", flag.ExitOnError)
		collectionListNameFlag                = collectionListFlags.String("name", "", "")
		collectionListOriginalIDFlag          = collectionListFlags.String("original-id", "", "")
		collectionListTransferIDFlag          = collectionListFlags.String("transfer-id", "", "")
		collectionListAipIDFlag               = collectionListFlags.String("aip-id", "", "")
		collectionListPipelineIDFlag          = collectionListFlags.String("pipeline-id", "", "")
		collectionListEarliestCreatedTimeFlag = collectionListFlags.String("earliest-created-time", "", "")
		collectionListLatestCreatedTimeFlag   = collectionListFlags.String("latest-created-time", "", "")
		collectionListStatusFlag              = collectionListFlags.String("status", "", "")
		collectionListCursorFlag              = collectionListFlags.String("cursor", "", "")

		collectionShowFlags  = flag.NewFlagSet("show", flag.ExitOnError)
		collectionShowIDFlag = collectionShowFlags.String("id", "REQUIRED", "Identifier of collection to show")

		collectionDeleteFlags  = flag.NewFlagSet("delete", flag.ExitOnError)
		collectionDeleteIDFlag = collectionDeleteFlags.String("id", "REQUIRED", "Identifier of collection to delete")

		collectionCancelFlags  = flag.NewFlagSet("cancel", flag.ExitOnError)
		collectionCancelIDFlag = collectionCancelFlags.String("id", "REQUIRED", "Identifier of collection to remove")

		collectionRetryFlags  = flag.NewFlagSet("retry", flag.ExitOnError)
		collectionRetryIDFlag = collectionRetryFlags.String("id", "REQUIRED", "Identifier of collection to retry")

		collectionWorkflowFlags  = flag.NewFlagSet("workflow", flag.ExitOnError)
		collectionWorkflowIDFlag = collectionWorkflowFlags.String("id", "REQUIRED", "Identifier of collection to look up")

		collectionDownloadFlags  = flag.NewFlagSet("download", flag.ExitOnError)
		collectionDownloadIDFlag = collectionDownloadFlags.String("id", "REQUIRED", "Identifier of collection to look up")

		collectionDecideFlags    = flag.NewFlagSet("decide", flag.ExitOnError)
		collectionDecideBodyFlag = collectionDecideFlags.String("body", "REQUIRED", "")
		collectionDecideIDFlag   = collectionDecideFlags.String("id", "REQUIRED", "Identifier of collection to look up")

		collectionBulkFlags    = flag.NewFlagSet("bulk", flag.ExitOnError)
		collectionBulkBodyFlag = collectionBulkFlags.String("body", "REQUIRED", "")

		collectionBulkStatusFlags = flag.NewFlagSet("bulk-status", flag.ExitOnError)
	)
	pipelineFlags.Usage = pipelineUsage
	pipelineListFlags.Usage = pipelineListUsage
	pipelineShowFlags.Usage = pipelineShowUsage
	pipelineProcessingFlags.Usage = pipelineProcessingUsage

	batchFlags.Usage = batchUsage
	batchSubmitFlags.Usage = batchSubmitUsage
	batchStatusFlags.Usage = batchStatusUsage
	batchHintsFlags.Usage = batchHintsUsage

	collectionFlags.Usage = collectionUsage
	collectionMonitorFlags.Usage = collectionMonitorUsage
	collectionListFlags.Usage = collectionListUsage
	collectionShowFlags.Usage = collectionShowUsage
	collectionDeleteFlags.Usage = collectionDeleteUsage
	collectionCancelFlags.Usage = collectionCancelUsage
	collectionRetryFlags.Usage = collectionRetryUsage
	collectionWorkflowFlags.Usage = collectionWorkflowUsage
	collectionDownloadFlags.Usage = collectionDownloadUsage
	collectionDecideFlags.Usage = collectionDecideUsage
	collectionBulkFlags.Usage = collectionBulkUsage
	collectionBulkStatusFlags.Usage = collectionBulkStatusUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "pipeline":
			svcf = pipelineFlags
		case "batch":
			svcf = batchFlags
		case "collection":
			svcf = collectionFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "pipeline":
			switch epn {
			case "list":
				epf = pipelineListFlags

			case "show":
				epf = pipelineShowFlags

			case "processing":
				epf = pipelineProcessingFlags

			}

		case "batch":
			switch epn {
			case "submit":
				epf = batchSubmitFlags

			case "status":
				epf = batchStatusFlags

			case "hints":
				epf = batchHintsFlags

			}

		case "collection":
			switch epn {
			case "monitor":
				epf = collectionMonitorFlags

			case "list":
				epf = collectionListFlags

			case "show":
				epf = collectionShowFlags

			case "delete":
				epf = collectionDeleteFlags

			case "cancel":
				epf = collectionCancelFlags

			case "retry":
				epf = collectionRetryFlags

			case "workflow":
				epf = collectionWorkflowFlags

			case "download":
				epf = collectionDownloadFlags

			case "decide":
				epf = collectionDecideFlags

			case "bulk":
				epf = collectionBulkFlags

			case "bulk-status":
				epf = collectionBulkStatusFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "pipeline":
			c := pipelinec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = pipelinec.BuildListPayload(*pipelineListNameFlag, *pipelineListStatusFlag)
			case "show":
				endpoint = c.Show()
				data, err = pipelinec.BuildShowPayload(*pipelineShowIDFlag)
			case "processing":
				endpoint = c.Processing()
				data, err = pipelinec.BuildProcessingPayload(*pipelineProcessingIDFlag)
			}
		case "batch":
			c := batchc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "submit":
				endpoint = c.Submit()
				data, err = batchc.BuildSubmitPayload(*batchSubmitBodyFlag)
			case "status":
				endpoint = c.Status()
			case "hints":
				endpoint = c.Hints()
			}
		case "collection":
			c := collectionc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "monitor":
				endpoint = c.Monitor()
			case "list":
				endpoint = c.List()
				data, err = collectionc.BuildListPayload(*collectionListNameFlag, *collectionListOriginalIDFlag, *collectionListTransferIDFlag, *collectionListAipIDFlag, *collectionListPipelineIDFlag, *collectionListEarliestCreatedTimeFlag, *collectionListLatestCreatedTimeFlag, *collectionListStatusFlag, *collectionListCursorFlag)
			case "show":
				endpoint = c.Show()
				data, err = collectionc.BuildShowPayload(*collectionShowIDFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = collectionc.BuildDeletePayload(*collectionDeleteIDFlag)
			case "cancel":
				endpoint = c.Cancel()
				data, err = collectionc.BuildCancelPayload(*collectionCancelIDFlag)
			case "retry":
				endpoint = c.Retry()
				data, err = collectionc.BuildRetryPayload(*collectionRetryIDFlag)
			case "workflow":
				endpoint = c.Workflow()
				data, err = collectionc.BuildWorkflowPayload(*collectionWorkflowIDFlag)
			case "download":
				endpoint = c.Download()
				data, err = collectionc.BuildDownloadPayload(*collectionDownloadIDFlag)
			case "decide":
				endpoint = c.Decide()
				data, err = collectionc.BuildDecidePayload(*collectionDecideBodyFlag, *collectionDecideIDFlag)
			case "bulk":
				endpoint = c.Bulk()
				data, err = collectionc.BuildBulkPayload(*collectionBulkBodyFlag)
			case "bulk-status":
				endpoint = c.BulkStatus()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// pipelineUsage displays the usage of the pipeline command and its subcommands.
func pipelineUsage() {
	fmt.Fprintln(os.Stderr, `The pipeline service manages Archivematica pipelines.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] pipeline COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List all known pipelines`)
	fmt.Fprintln(os.Stderr, `    show: Show pipeline by ID`)
	fmt.Fprintln(os.Stderr, `    processing: List all processing configurations of a pipeline given its ID`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s pipeline COMMAND --help\n", os.Args[0])
}
func pipelineListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] pipeline list", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -status BOOL")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all known pipelines`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -status BOOL: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "pipeline list --name \"abc123\" --status false")
}

func pipelineShowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] pipeline show", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Show pipeline by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Identifier of pipeline to show`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "pipeline show --id \"d1845cb6-a5ea-474a-9ab8-26f9bcd919f5\"")
}

func pipelineProcessingUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] pipeline processing", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all processing configurations of a pipeline given its ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Identifier of pipeline`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "pipeline processing --id \"d1845cb6-a5ea-474a-9ab8-26f9bcd919f5\"")
}

// batchUsage displays the usage of the batch command and its subcommands.
func batchUsage() {
	fmt.Fprintln(os.Stderr, `The batch service manages batches of collections.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] batch COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    submit: Submit a new batch`)
	fmt.Fprintln(os.Stderr, `    status: Retrieve status of current batch operation.`)
	fmt.Fprintln(os.Stderr, `    hints: Retrieve form hints`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s batch COMMAND --help\n", os.Args[0])
}
func batchSubmitUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] batch submit", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Submit a new batch`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "batch submit --body '{\n      \"completed_dir\": \"abc123\",\n      \"depth\": 1,\n      \"exclude_hidden_files\": false,\n      \"path\": \"abc123\",\n      \"pipeline\": \"abc123\",\n      \"process_name_metadata\": false,\n      \"processing_config\": \"abc123\",\n      \"reject_duplicates\": false,\n      \"retention_period\": \"abc123\",\n      \"transfer_type\": \"abc123\"\n   }'")
}

func batchStatusUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] batch status", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retrieve status of current batch operation.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "batch status")
}

func batchHintsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] batch hints", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retrieve form hints`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "batch hints")
}

// collectionUsage displays the usage of the collection command and its
// subcommands.
func collectionUsage() {
	fmt.Fprintln(os.Stderr, `The collection service manages packages being transferred to Archivematica.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] collection COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    monitor: Monitor implements monitor.`)
	fmt.Fprintln(os.Stderr, `    list: List all stored collections`)
	fmt.Fprintln(os.Stderr, `    show: Show collection by ID`)
	fmt.Fprintln(os.Stderr, `    delete: Delete collection by ID`)
	fmt.Fprintln(os.Stderr, `    cancel: Cancel collection processing by ID`)
	fmt.Fprintln(os.Stderr, `    retry: Retry collection processing by ID`)
	fmt.Fprintln(os.Stderr, `    workflow: Retrieve workflow status by ID`)
	fmt.Fprintln(os.Stderr, `    download: Download collection by ID`)
	fmt.Fprintln(os.Stderr, `    decide: Make decision for a pending collection by ID`)
	fmt.Fprintln(os.Stderr, `    bulk: Bulk operations (retry, cancel...).`)
	fmt.Fprintln(os.Stderr, `    bulk-status: Retrieve status of current bulk operation.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s collection COMMAND --help\n", os.Args[0])
}
func collectionMonitorUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection monitor", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Monitor implements monitor.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection monitor")
}

func collectionListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection list", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -original-id STRING")
	fmt.Fprint(os.Stderr, " -transfer-id STRING")
	fmt.Fprint(os.Stderr, " -aip-id STRING")
	fmt.Fprint(os.Stderr, " -pipeline-id STRING")
	fmt.Fprint(os.Stderr, " -earliest-created-time STRING")
	fmt.Fprint(os.Stderr, " -latest-created-time STRING")
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -cursor STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all stored collections`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -original-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -transfer-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -aip-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -pipeline-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -earliest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -latest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -cursor STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection list --name \"abc123\" --original-id \"abc123\" --transfer-id \"d1845cb6-a5ea-474a-9ab8-26f9bcd919f5\" --aip-id \"d1845cb6-a5ea-474a-9ab8-26f9bcd919f5\" --pipeline-id \"d1845cb6-a5ea-474a-9ab8-26f9bcd919f5\" --earliest-created-time \"e1d563b0-1474-4155-beed-f2d3a12e1529\" --latest-created-time \"e1d563b0-1474-4155-beed-f2d3a12e1529\" --status \"in progress\" --cursor \"abc123\"")
}

func collectionShowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection show", os.Args[0])
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Show collection by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to show`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection show --id 1")
}

func collectionDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete collection by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to delete`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection delete --id 1")
}

func collectionCancelUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection cancel", os.Args[0])
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Cancel collection processing by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to remove`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection cancel --id 1")
}

func collectionRetryUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection retry", os.Args[0])
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retry collection processing by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to retry`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection retry --id 1")
}

func collectionWorkflowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection workflow", os.Args[0])
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retrieve workflow status by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to look up`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection workflow --id 1")
}

func collectionDownloadUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection download", os.Args[0])
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Download collection by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to look up`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection download --id 1")
}

func collectionDecideUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection decide", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id UINT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Make decision for a pending collection by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id UINT: Identifier of collection to look up`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection decide --body '{\n      \"option\": \"abc123\"\n   }' --id 1")
}

func collectionBulkUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection bulk", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Bulk operations (retry, cancel...).`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection bulk --body '{\n      \"operation\": \"cancel\",\n      \"size\": 1,\n      \"status\": \"in progress\"\n   }'")
}

func collectionBulkStatusUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] collection bulk-status", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retrieve status of current bulk operation.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "collection bulk-status")
}
